<UserControl x:Class="Northwind.UI.WPF.CustomerSearchBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:UI="clr-namespace:Northwind.Framework.UI;assembly=Northwind.Framework.UI" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>   
        <ListBox ItemsSource="{Binding Customers}"
                 SelectedValuePath="CustomerID"
                 SelectedItem="{Binding SelectedCustomer}"
                 HorizontalAlignment="Left"
                 Height="180"
                 Width="250">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding CompanyName}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.Resources>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="UI:ControlBehaviors.MouseDoubleClickCommand"
                            Value="{Binding ShowCustomerDetailsCommand}" />
                </Style>
            </ListBox.Resources>
        </ListBox>
    </StackPanel>
</UserControl>
